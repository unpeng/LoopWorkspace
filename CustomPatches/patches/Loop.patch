diff --git a/Loop/Localizable.xcstrings b/Loop/Localizable.xcstrings
index c7150a85..d36e8d90 100644
--- a/Loop/Localizable.xcstrings
+++ b/Loop/Localizable.xcstrings
@@ -15014,12 +15014,18 @@
         }
       }
     },
+    "Customize your Loop experience by adjusting additional settings" : {
+      "comment" : "Descriptive text for Preferences"
+    },
     "Daily glucose pattern" : {
       "comment" : "Accessibility label for the daily glucose pattern (AGP) chart"
     },
     "Daily Glucose Pattern" : {
       "comment" : "Daily glucose pattern (AGP) section header"
     },
+    "Damper Strength: %1$@%%" : {
+      "comment" : "Format string describing damper strength. (1: damper strength percentage)"
+    },
     "Date" : {
       "comment" : "Date picker label",
       "localizations" : {
@@ -17592,6 +17598,9 @@
         }
       }
     },
+    "Enable Negative Insulin Damper" : {
+      "comment" : "Title for Negative Insulin Damper toggle"
+    },
     "Enabled" : {
       "comment" : "Title for enable live activity toggle",
       "localizations" : {
@@ -28045,6 +28054,12 @@
         }
       }
     },
+    "Negative Insulin Damper" : {
+      "comment" : "Title for negative insulin damper experiment description\nTitle of negative insulin damper experiment\nTitle of the prediction input effect for negative insulin damper"
+    },
+    "Negative Insulin Damper (NID) is used to mitigate the effects of temporarily increased insulin sensitivity. Such increases can result in spending significant times beneath target and eventually going low. Loop may erroneously predict glucose going too high, resulting in excess insulin being delivered. To counteract this, NID acts as a dynamic damper on increases to  predicted glucose. The strength of this damper is controlled by the total predicted rise in glucose due to negative insulin. The greater the amount of negative insulin, the stronger the damper and the bigger the reductions. The calculation is done with a 15 minute lag." : {
+      "comment" : "Description of Negative Insulin Damper toggle."
+    },
     "New Favorite Food" : {
       "comment" : "Title of new favorite food screen",
       "localizations" : {
@@ -31270,6 +31285,9 @@
         }
       }
     },
+    "Preferences" : {
+      "comment" : "Title text for button to Preferences"
+    },
     "Preparing Critical Event Logs" : {
       "comment" : "Preparing critical event log text",
       "localizations" : {
@@ -31643,6 +31661,9 @@
       "comment" : "The title of the text field where the user can set the name of a glucose alert profile.",
       "isCommentAutoGenerated" : true
     },
+    "Profiles" : {
+      "comment" : "Title text for button to Profiles"
+    },
     "Pump" : {
       "comment" : "The title of the pump section in settings",
       "extractionState" : "manual",
@@ -33789,6 +33810,9 @@
     },
     "Recommended: " : {
 
+    },
+    "Reduces increases in glucose. The damper is stronger when there is more negative insulin" : {
+      "comment" : "Description of the prediction input effect for negative insulin damper"
     },
     "References" : {
       "comment" : "A heading for a list of references.",
@@ -36480,6 +36504,9 @@
     },
     "Swimming" : {
 
+    },
+    "Switch between profiles for different scenarios" : {
+      "comment" : "Descriptive text for Profiles"
     },
     "Switch to main" : {
       "comment" : "Button on the development build warning that opens documentation about switching branches"
diff --git a/Loop/Managers/LoopDataManager.swift b/Loop/Managers/LoopDataManager.swift
index 8797bcd8..cd00798f 100644
--- a/Loop/Managers/LoopDataManager.swift
+++ b/Loop/Managers/LoopDataManager.swift
@@ -1327,7 +1327,33 @@ extension LoopDataManager: ServicesManagerDelegate {
     func cancelCurrentOverride() async throws {
         temporaryPresetsManager.scheduleOverride = nil
     }
-    
+
+    //wp begin
+    // 远程 AutomaticDosingStrategy 指令。settingsProvider 只暴露读接口，
+    // 真正的实现方 SettingsManager 才有 mutateLoopSettings，故在此向下转型，
+    // 避免改动上游的 SettingsProvider 协议。
+    func enactRemoteDosingStrategy(automaticDosingStrategy: AutomaticDosingStrategy) async throws {
+        guard let settingsManager = settingsProvider as? SettingsManager else {
+            throw EnactDosingStrategyError.settingsUnavailable
+        }
+
+        settingsManager.mutateLoopSettings { settings in
+            settings.automaticDosingStrategy = automaticDosingStrategy
+        }
+    }
+
+    enum EnactDosingStrategyError: LocalizedError {
+
+        case settingsUnavailable
+
+        var errorDescription: String? {
+            switch self {
+            case .settingsUnavailable:
+                return NSLocalizedString("Settings are not available to change the dosing strategy.", comment: "Error description when remote dosing strategy cannot be applied")
+            }
+        }
+    }
+    //wp end
 
     enum EnactOverrideError: LocalizedError {
         
diff --git a/Loop/Managers/RemoteDataServicesManager.swift b/Loop/Managers/RemoteDataServicesManager.swift
index 11712704..8991250a 100644
--- a/Loop/Managers/RemoteDataServicesManager.swift
+++ b/Loop/Managers/RemoteDataServicesManager.swift
@@ -391,7 +391,14 @@ extension RemoteDataServicesManager {
                 case .failure(let error):
                     self.log.error("Error querying dosing decision data: %{public}@", String(describing: error))
                     semaphore.signal()
-                case .success(let queryAnchor, let data):
+                case .success(let queryAnchor, var data):
+                    //wp begin
+                    // next-dev 把 settingsStore 换成了 settingsProvider，settings 也不再是可选值
+                    let automaticDosingStrategy = self.settingsProvider.settings.automaticDosingStrategy == AutomaticDosingStrategy.automaticBolus ? "automaticBolus" : "tempBasalOnly"
+                    for(index, _) in data.enumerated() {
+                        data[index].automaticDosingStrategy = automaticDosingStrategy
+                    }
+                    //wp end
                     Task {
                         do {
                             try await remoteDataService.uploadDosingDecisionData(data)
diff --git a/Loop/Managers/ServicesManager.swift b/Loop/Managers/ServicesManager.swift
index c7252128..f52a7b65 100644
--- a/Loop/Managers/ServicesManager.swift
+++ b/Loop/Managers/ServicesManager.swift
@@ -251,6 +251,9 @@ public protocol ServicesManagerDelegate: AnyObject {
     func enactOverride(name: String, duration: TemporaryScheduleOverride.Duration?, remoteAddress: String) async throws
     func cancelCurrentOverride() async throws
     func deliverCarbs(amountInGrams: Double, absorptionTime: TimeInterval?, foodType: String?, startDate: Date?) async throws
+    //wp begin
+    func enactRemoteDosingStrategy(automaticDosingStrategy: AutomaticDosingStrategy) async throws
+    //wp end
 }
 
 // MARK: - StatefulPluggableDelegate
@@ -361,6 +364,19 @@ extension ServicesManager: ServiceDelegate {
         }
     }
     
+    //wp begin
+    func deliverRemoteBolus(amountInUnits: Double, amountInGrams: Double, absorptionTime: TimeInterval?, foodType: String?, startDate: Date?) async throws {
+        try await deliverRemoteCarbs(amountInGrams: amountInGrams, absorptionTime: absorptionTime, foodType: foodType, startDate: startDate)
+        // next-dev 给 deliverRemoteBolus 增加了 decisionId 参数
+        try await deliverRemoteBolus(amountInUnits: amountInUnits, decisionId: nil)
+    }
+    
+    func enactRemoteDosingStrategy(automaticDosingStrategy: AutomaticDosingStrategy) async throws {
+        try await servicesManagerDelegate?.enactRemoteDosingStrategy(automaticDosingStrategy: automaticDosingStrategy)
+        await remoteDataServicesManager.triggerUpload(for: .settings)
+    }
+    //wp end
+    
     enum BolusActionError: LocalizedError {
         
         case invalidBolus
diff --git a/Loop/Models/LoopConstants.swift b/Loop/Models/LoopConstants.swift
index 5a78c4e8..664c7e63 100644
--- a/Loop/Models/LoopConstants.swift
+++ b/Loop/Models/LoopConstants.swift
@@ -15,9 +15,9 @@ enum LoopConstants {
     
     // Input field bounds
     
-    static let maxCarbEntryQuantity = LoopQuantity(unit: .gram, doubleValue: 250) // cannot exceed this value
+    static let maxCarbEntryQuantity = LoopQuantity(unit: .gram, doubleValue: 99) // cannot exceed this value
 
-    static let warningCarbEntryQuantity = LoopQuantity(unit: .gram, doubleValue: 99) // user is warned above this value
+    static let warningCarbEntryQuantity = LoopQuantity(unit: .gram, doubleValue: 49) // user is warned above this value
     
     static let validManualGlucoseEntryRange = LoopQuantity(unit: .milligramsPerDeciliter, doubleValue: 10)...LoopQuantity(unit: .milligramsPerDeciliter, doubleValue: 600)
     
@@ -25,7 +25,7 @@ enum LoopConstants {
     static let maxCarbAbsorptionTime = TimeInterval(hours: 8)
     
     static let maxCarbEntryPastTime = TimeInterval(hours: (-12))
-    static let maxCarbEntryFutureTime = TimeInterval(hours: 1)
+    static let maxCarbEntryFutureTime = TimeInterval(hours: 4)
 
     static let maxOverrideDurationTime = TimeInterval(hours: 24)
     
